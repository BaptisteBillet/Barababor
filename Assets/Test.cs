using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {

        //Debug.Log(Google2u.Element.Instance.GetRow(Google2u.Element.rowIds.Coque)._Name);

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
